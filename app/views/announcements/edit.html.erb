<% if admin_signed_in? %>

<p id="notice"><%= notice %></p>

 <!-- Page Content -->
    <div class="container">

        <!-- Page Heading/Breadcrumbs -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">HHSYC - Administrators View
                    <!-- <small>founded in 2001</small> -->
                </h1>
                <ol class="breadcrumb">
                    <li><%=link_to 'Admin Home Page', adminfisher_path %>
                    </li>  
                     <ul>
                          <li class="go-right">
                             <%= link_to "Logout", destroy_admin_session_path, class: "active", method: :delete %>
                          </li>
                     </ul>  
                </ol>
            </div>
        </div>
        <!-- /.row -->

        <!-- Content Row -->
        <div class="row">
            <!-- Sidebar Column -->
                <%= render 'shared/sidebar_admin' %>
            <!-- Content Column -->
            <div class="col-md-9">
                <div class="stylestatic">
                    <h3>Editing Announcement</h3>
                        <%= render 'form' %>
                        <%= link_to 'View', @announcement %> |
        				<%= link_to 'Back', announcements_path %>
                </div>  
            </div>
        </div>
        <!-- /.row -->

        <hr class="featurette-divider">
            <% else %>

            <%= get '/401', :to => redirect('/401.html') %>

            <% end %>
    </div>


