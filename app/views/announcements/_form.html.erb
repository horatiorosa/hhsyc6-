<%= form_for(@announcement) do |f| %>
  <% if @announcement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@announcement.errors.count, "error") %> prohibited this announcement from being saved:</h2>

      <ul>
      <% @announcement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.file_field :image, placeholder: "Upload a photo here (or leave blank to upload an image later" %>
    <%= f.hidden_field :image_cache %>
    <%= f.text_field :ann_title, placeholder: "Enter your announcement or event title here" %>
    <%= f.text_area :ann_body, placeholder: "Enter your announcement or event details here", size: "50x20" %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
